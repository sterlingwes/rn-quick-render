# Gradle-populated runtime bundle. Not checked in — re-stage with
# `./gradlew :renderer:packageForNpm` (host target) or `-Ptarget=linux`
# (cross-target). When the package is published to npm, `dist*/` is
# included via the `files` array in package.json.
dist/
dist-*/
node_modules/

# Gradle-populated runtime bundle. Not checked in — re-stage with
# `./gradlew :renderer:packageForNpm`. When the package is published
# to npm, `dist/` is included via the `files` array in package.json.
dist/
node_modules/
